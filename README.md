# Underskeleton Website

## Jekyll Command

Run the command from inside the root directory: `underskeleton-gh-pages`
`jekyll build --watch`

## Browser Sync command

Run the command from `_site` folder.
```
cd _site
browser-sync start --server --files "**/*.*"
```
